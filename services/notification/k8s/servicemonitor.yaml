apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: notification
  labels:
    release: monitoring
spec:
  selector:
    matchLabels:
      app: notification
  endpoints:
    - port: http
      path: /actuator/prometheus
      interval: 15s
