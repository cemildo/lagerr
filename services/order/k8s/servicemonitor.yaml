apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: order
  labels:
    release: monitoring
spec:
  selector:
    matchLabels:
      app: order
  endpoints:
    - port: http
      path: /actuator/prometheus
      interval: 15s
